# GH-81 — refactor: architecture agent-centric pour les agents métiers IA

## Contexte
Refacto déjà commité (commit `7d61f16`). Ce ticket documente le travail effectué et les améliorations restantes éventuelles. Les agents métiers (buffett, lynch, quick-analyse) ont été migrés vers une architecture avec frontmatter standardisé, alignée sur `.claude/agents/`.

## Ce qui a été fait
- [ ] `src/agents/buffett-analyse.md` — frontmatter ajouté (name, description, model, tools)
- [ ] `src/agents/lynch-analyse.md` — frontmatter ajouté
- [ ] `src/agents/quick-analyse.md` — frontmatter ajouté
- [ ] `src/lib/agent-loader.ts` — nouveau module : parse frontmatter + résolution model ID
- [ ] `src/app/api/analyse/classic/route.ts` — utilise `loadAgent()` + `resolveModelId()`
- [ ] `src/app/api/analyse/ticker/route.ts` — utilise `loadAgent()` + `resolveModelId()`

## Fichiers concernés
| Fichier | Action |
|---------|--------|
| `src/agents/buffett-analyse.md` | modifié — frontmatter ajouté |
| `src/agents/lynch-analyse.md` | modifié — frontmatter ajouté |
| `src/agents/quick-analyse.md` | modifié — frontmatter ajouté |
| `src/lib/agent-loader.ts` | créé — loader agent centralisé |
| `src/app/api/analyse/classic/route.ts` | modifié — utilise agent-loader |
| `src/app/api/analyse/ticker/route.ts` | modifié — utilise agent-loader |

## Critères d'acceptation
- [ ] CA1 : Changer le modèle d'un agent se fait uniquement dans le frontmatter de son fichier `.md`
- [ ] CA2 : Le mapping `model-name → API ID` est centralisé dans `resolveModelId()` de `agent-loader.ts`
- [ ] CA3 : Les routes `/api/analyse/classic` et `/api/analyse/ticker` utilisent `loadAgent()`
- [ ] CA4 : `GOOGLE_AI_API_KEY` est référencée via `process.env` uniquement (pas hardcodée)

## Hors périmètre
- L'ajout de nouveaux agents
- La modification du comportement des analyses existantes
- La migration d'autres routes API

## Notes techniques
- Commit de référence : `7d61f16`
- Ce ticket est de type refactor — pas de nouvelle fonctionnalité utilisateur
- Sans label de priorité — amélioration technique interne

## Dépendances
- Bloquée par : aucune (déjà livré)
- Débloque : toute future évolution des agents métiers

## Estimation
XS — déjà implémenté, ticket documentaire
