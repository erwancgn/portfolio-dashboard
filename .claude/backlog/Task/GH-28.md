# GH-28 — [TASK-010] Spending limit IA + compteur tokens

## Contexte
Partie de l'EPIC 5 Infrastructure (GH-5). Protège contre une dérive de coût IA (estimé ~7€/mois). Complément à GH-33 qui ajoute un compteur dans le chat.

## Description
En tant que PO, je veux voir ma consommation IA dans l'app et avoir une protection contre les dépassements de budget, afin de ne pas avoir de mauvaises surprises sur la facture Anthropic.

## Ce qui doit être fait
- [ ] Créer la migration DB pour la table `api_usage` (user_id, model, tokens_input, tokens_output, cost_eur, called_at, endpoint)
- [ ] Créer `src/lib/usage-tracker.ts` — logger chaque appel IA avec coût estimé
- [ ] Modifier les routes IA pour appeler le tracker après chaque requête
- [ ] Créer `src/components/settings/UsageWidget.tsx` — affichage consommation mensuelle
- [ ] Ajouter la section Settings ou widget dans le dashboard

## Fichiers concernés
| Fichier | Action |
|---------|--------|
| `supabase/migrations/` | créer — table `api_usage` |
| `src/lib/usage-tracker.ts` | créer — module de tracking |
| `src/app/api/analyse/chat/route.ts` | modifier — appel usage-tracker |
| `src/app/api/analyse/classic/route.ts` | modifier — appel usage-tracker |
| `src/app/api/analyse/ticker/route.ts` | modifier — appel usage-tracker |
| `src/components/settings/UsageWidget.tsx` | créer — widget consommation |
| `src/types/database.ts` | régénérer après migration |

## Critères d'acceptation
- [ ] CA1 : Chaque appel IA (chat, analyse) est loggué en base avec tokens et coût estimé
- [ ] CA2 : La consommation mensuelle totale est visible dans l'app (€ et tokens)
- [ ] CA3 : Le spending limit est configuré sur le dashboard Anthropic (action manuelle documentée)
- [ ] CA4 : L'estimation de coût est cohérente avec les tarifs réels (Haiku vs Sonnet)

## Hors périmètre
- Le blocage automatique des appels si budget dépassé (piloté par Anthropic spending limit)
- Les alertes email de budget
- Le tracking des appels FMP et Yahoo Finance

## Notes techniques
- Table `api_usage` : `id, user_id, model, endpoint, tokens_input, tokens_output, cost_eur, called_at`
- Coûts estimés à hardcoder : Haiku ~0.0003$/1k tokens, Sonnet ~0.003$/1k tokens
- Le spending limit Anthropic est une action manuelle (dashboard Anthropic) — le documenter dans le widget
- `src/lib/ai.ts` existe — passer par ce module pour centraliser le tracking

## Dépendances
- Bloquée par : aucune
- Débloque : rien (GH-33 peut utiliser ce système pour son compteur)

## Estimation
M — migration DB + nouveau module + modification 3 routes + composant UI
