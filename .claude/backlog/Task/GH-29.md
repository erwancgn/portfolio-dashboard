# GH-29 — [TASK-011] Tests unitaires

## Contexte
Partie de l'EPIC 5 Infrastructure (GH-5). Les calculs financiers (PRU, P&L, poids, DCA) sont critiques — une erreur entraîne une mauvaise décision d'investissement. Des tests unitaires garantissent la fiabilité de ces calculs.

## Description
En tant que développeur, je veux une suite de tests unitaires sur les calculs financiers critiques, afin de garantir qu'une régression ne passe pas inaperçue.

## Ce qui doit être fait
- [ ] Installer Vitest et configurer `vitest.config.ts`
- [ ] Écrire les tests pour `src/lib/calculations.ts` si le fichier existe (PRU, P&L, poids)
- [ ] Écrire les tests pour la logique DCA (calcul PRU, next_expected_at)
- [ ] Écrire les tests pour `src/lib/fiscal.ts` (règles CTO, PEA, Crypto) — compléter `src/lib/fiscal.test.ts`
- [ ] Ajouter le script `test` dans `package.json`

## Fichiers concernés
| Fichier | Action |
|---------|--------|
| `package.json` | modifier — ajouter script test + dépendance vitest |
| `vitest.config.ts` | créer — configuration Vitest |
| `src/lib/fiscal.test.ts` | modifier — compléter les tests existants |
| `src/lib/fmp-dividends.test.ts` | vérifier — tests existants |
| `src/lib/ai-validation.test.ts` | vérifier — tests existants |
| `src/lib/calculations.test.ts` | créer si `calculations.ts` existe |

## Critères d'acceptation
- [ ] CA1 : `npm run test` s'exécute sans erreur
- [ ] CA2 : Couverture > 80% sur les fichiers dans `src/lib/`
- [ ] CA3 : Les tests couvrent les cas limites : PRU avec quantité zéro, P&L négatif, allocation à 100%
- [ ] CA4 : Les tests existants (`fiscal.test.ts`, `fmp-dividends.test.ts`, `ai-validation.test.ts`) passent toujours

## Hors périmètre
- Les tests d'intégration (routes API)
- Les tests end-to-end (Playwright/Cypress)
- Les tests des composants React

## Notes techniques
- Vitest est recommandé pour les projets Next.js (compatible ESM, rapide)
- Les fichiers `*.test.ts` existent déjà — les lire avant d'écrire de nouveaux tests
- Vérifier l'existence de `src/lib/calculations.ts` avant d'écrire les tests associés
- `src/lib/ai.test.ts` existe aussi — vérifier son état

## Dépendances
- Bloquée par : aucune
- Débloque : rien

## Estimation
M — configuration Vitest + écriture des tests sur plusieurs fichiers
